<!DOCTYPE html>
<html lang="en">
<%- include ('partials/head') %>
<body>
 <div class="  border w-75 mx-auto rounded container my-5">
  


<div class="my-6 px-5 py-3">
  <h1 class="card-title mt-4 mb-4 text-center">To Do List</h1>

  <form action="/update/<%= task.id %>" method="post" class="w-75 mx-auto">
    <div class="mb-3">
      <label for="title" class="form-label">Title:</label>
      <input 
        type="text" 
        class="form-control" 
        id="title" 
        name="title"
        placeholder="Enter title "
        value="<%= task.title %>"
      />
    </div>

    <div class="mb-4">
      <label for="description" class="form-label">Description:</label>
      <textarea 
        class="form-control" 
        id="description" 
        name="description"
        rows="5" 
        placeholder="Type your message here..."
        
      ><%= task.description %></textarea>
    </div>

    <div class="text-center">
      <button type="submit" class="btn w-50 btn-primary py-2">Edit</button>
    </div>
  </form>
</div>





</div>

</div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.7/js/bootstrap.min.js"></script>
</body>
</html>